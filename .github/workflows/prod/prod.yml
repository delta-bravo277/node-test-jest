name: Prod CI
on: 
  push:
    branches:
      - dev
    paths:
      - ".github/workflows/prod/prod.yml"
  pull_request:
    types:
      - closed
    branches:
      - main
jobs:
  CI:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: "CI+CD"
        run: |
          echo "PROD Success!