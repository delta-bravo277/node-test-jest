name: Node.js CI

on: [push]

jobs:
  CI:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Use Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '12'
      - run: npm install
      # - run: npm run build --if-present
      - run: npm run test
  TAG:
    needs: CI
    runs-on: ubuntu-latest
    outputs:
      tag: ${{ steps.get-tag.outputs.part }}
    steps:
      - uses: actions/checkout@v3

      - name: Assign TAG
        id: get-tag
        uses: delta-bravo277/github-action-tag@main
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          WITH_V: true
          DEFAULT_BUMP: none

  CD:
    needs: [TAG]
    if: ${{ needs.TAG.outputs.tag != '0' || needs.TAG.outputs.tag != '' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: "CI+CD"
        run: echo "Success! CI+CD"
    